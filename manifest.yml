info:
  name: secret-man
  description: "Secret man"
  version: "1.1"
  category: infra

include:
  - file: /etc/serve/include.d/library.yml

build:
  - sh: "make docker-dist VERSION={{ info.build-version }}"
  - sh: 'curl -sSf -uci:${ART_CI_PASSWORD} -T bin/secret-man-v{{ info.build-version }}-linux-amd64 "http://art.boople.co/artifactory/infra/secret-man/"'
